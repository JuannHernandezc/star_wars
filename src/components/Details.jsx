import React from 'react'
const Details = ({characterId,name,birthYear,eyeColor,gender,hairColor,height,filmConnection}) => {
    return(
        <div>
          <h1>{name}</h1>
        </div>
    )
}

export default Details

